{
  "name": "for-the-hour-web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite dev",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && wrangler deploy",
    "check": "svelte-kit sync && svelte-check --tsconfig ./jsconfig.json",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@sveltejs/adapter-static": "^3.0.10",
    "@sveltejs/kit": "^2.57.1",
    "@tailwindcss/vite": "^4.2.2",
    "svelte": "^5.55.3",
    "tailwindcss": "^4.2.2",
    "vite": "^8.0.8",
    "wrangler": "^4.81.1"
  }
}
